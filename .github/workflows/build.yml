name: Build ImmortalWrt for NanoPi R1S-H3

on:
  workflow_dispatch:
    inputs:
      custom_feed_url:
        description: '添加自定义软件源地址 (例如: https://github.com/...)'
        required: false
        default: ''
        type: string

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Source
        uses: actions/checkout@v5

      - name: Initialization Environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison gawk g++-multilib g++ \
            gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev \
            file wget swig libelf-dev libgmp-dev libltdl-dev libmpc-dev libmpfr-dev \
            libtool ninja-build p7zip p7zip-full qemu-utils u-boot-tools upx-ucl xxd

      - name: Add Custom Feeds from Web Input
        run: |
          if [ -n "${{ inputs.custom_feed_url }}" ]; then
            echo "检测到自定义源输入，正在添加: ${{ inputs.custom_feed_url }}"
            echo "src-git custom_feed ${{ inputs.custom_feed_url }}" >> feeds.conf.default
          else
            echo "未输入自定义源，跳过..."
          fi
          
          echo "=== 当前 Feeds 配置文件内容 ==="
          cat feeds.conf.default

      - name: Update Feeds
        run: |
          # 核心修复：直接把会引发全网依赖警告、缺失 libwayland 以及递归死锁的 video、telephony 软件源从源头抹掉
          sed -i '/telephony/d' feeds.conf.default
          sed -i '/video/d' feeds.conf.default
          
          echo "=== 清理冗余源后的 Feeds 配置 ==="
          cat feeds.conf.default

          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Update Configuration and DTS
        run: |
          cp r1s-h3.config.seed .config
          
          # 1. 自动修复 DTS 设备树文件中的 mmc2 (SDIO WiFi) 禁用状态为 okay
          DTS_FILE=$(find target/linux/sunxi/ -iname "*nanopi*r1s*.dts" 2>/dev/null | head -n 1)
          if [ -n "$DTS_FILE" ]; then
            sed -i '/&mmc2/,/};/s/status = "disabled";/status = "okay";/' "$DTS_FILE"
          fi
          
          # 2. 植入开机固定 MAC、静态 IP、DHCP 及 mDNS 主机名广播初始化脚本
          mkdir -p files/etc/uci-defaults
          cat << 'EOF' > files/etc/uci-defaults/99-custom-network-settings
          #!/bin/sh
          
          # 设置网络与固定桥接 LAN
          uci set network.lan=interface
          uci set network.lan.device='br-lan'
          uci set network.lan.proto='static'
          uci set network.lan.ipaddr='192.168.1.1'
          uci set network.lan.netmask='255.255.255.0'
          uci set network.lan.stp='0'
          uci set network.lan.delegate='0'
          uci set network.lan.force_link='1'
          
          uci set network.@device[0]=device
          uci set network.@device[0].name='br-lan'
          uci set network.@device[0].type='bridge'
          uci set network.@device[0].macaddr='02:42:ac:11:00:02'
          
          # 设置内置 DHCP 服务
          uci set dhcp.lan=dhcp
          uci set dhcp.lan.interface='lan'
          uci set dhcp.lan.start='100'
          uci set dhcp.lan.limit='150'
          uci set dhcp.lan.leasetime='12h'
          uci set dhcp.lan.authoritative='1'
          uci set dhcp.lan.force='1'
          
          # 设定默认主机名为 nanopi-r1
          uci set system.@system[0].hostname='nanopi-r1'
          
          uci commit network
          uci commit dhcp
          uci commit system
          
          # 确保开机自启 avahi-daemon 服务
          /etc/init.d/avahi-daemon enable
          /etc/init.d/avahi-daemon start || true
          EOF
          chmod +x files/etc/uci-defaults/99-custom-network-settings
          
          make defconfig

      - name: Download Package Toolchain
        run: |
          make download -j8
          find dl -size -1024c -exec rm -f {} \;

      - name: Compile Firmware
        run: |
          make -j$(nproc) || make -j1 V=s

      - name: Organize Artifacts
        run: |
          mkdir -p bin/out
          
          echo "=== 正在精准提取 NanoPi R1 固件及 Armbian Docker 容器包 ==="
          BUILD_DATE=$(date +%Y%m%d)
          
          for file in bin/targets/sunxi/cortexa7/*nanopi-r1*.img.gz; do
            if [ -f "$file" ]; then
              filename=$(basename "$file")
              new_filename="${filename%.img.gz}-$BUILD_DATE.img.gz"
              cp -f "$file" "bin/out/$new_filename"
              echo "已重命名镜像: $filename -> $new_filename"
            fi
          done
          
          for file in bin/targets/sunxi/cortexa7/*rootfs.tar.gz; do
            if [ -f "$file" ]; then
              filename=$(basename "$file")
              new_filename="immortalwrt-sunxi-cortexa7-rootfs-$BUILD_DATE.tar.gz"
              cp -f "$file" "bin/out/$new_filename"
              echo "已提取 Docker 容器包: $filename -> $new_filename"
            fi
          done
          
          if [ -f "bin/targets/sunxi/cortexa7/sha256sums" ]; then
            cp -f bin/targets/sunxi/cortexa7/sha256sums bin/out/
          fi
          
          echo "=== 打包输出目录清单 ==="
          ls -lh bin/out/

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ImmortalWrt-NanoPi-R1S-H3
          path: bin/out/*
